"""Django app configuration for kante."""

from django.apps import AppConfig


class KanteConfig(AppConfig):
    """App configuration for kante."""

    """App configuration for kante."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "kante"
